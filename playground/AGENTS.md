# Playground

The playground is a Strapi app that uses the Meilisearch Plugin implemented in this repository.

## Database seeding

- The database is seeded manually. Do not update it.
- You can use `sqlite3` to read the state of the database.
